// Comment

// Variable
var variable1 = "value";
let variable2 = "value";

// Const
const constant = "value";

// Datatype
let number = 12345;
let string = "abcd";
let boolean = true;

// Object
let object = {
  key: "value",
  "key name": "value",
};

// Array
let array = [123, "abcd", true];

// Function
function functionName(parameter) {
  console.log(parameter);
}

// Arrow Function
let arrowFunction = (parameter) => {
  console.log(parameter);
};

// Constructor
let myMap = new Map();

// Constructor Function
function ConstructorFunction(parameter) {
  this.parameter = parameter;
  return console.log(parameter);
}

// Class
class MyClass {
  constructor(parameter) {
    this.variable = parameter;
    function method() {}
  }
}

// Object
let myObject = new MyClass("value");

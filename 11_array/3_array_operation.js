/* ----------------------------- Array Operation ---------------------------- */

const array = [1, 2, 3];

console.log(array[0]); // Accessing element
array[3] = 4; // Adding element
array[0] = -1; // Changing/ Updating element
array.push(5); // Pushing element
array.pop(3); // Popping element

// // Shift element to array.
// students.shift("Student1");
// console.log(students);

// // Unshift element to array.
// students.unshift("Student1");
// console.log(students);

// // Spread operator in array.
// const classRoom = [...students];
// // classRoom = [...students, "Student4"];
// console.log(classRoom);

// `in` Operator (Membership Operator)

// What
// - Checks if a property exists as a direct own property of an object.

// Example
let person = { name: "Alice", age: 30 };

console.log("name" in person); // Output: true (name is a direct property)
console.log("age" in person); // Output: true (age is a direct property)

// Push Method In Array

/* 
    # Syntax
    datatype arrayName = [Item1,Item2, Item3];
    arrayName.push(value);

    !Note:
    - It will only add element at the last in the array.
*/

const colors = ["Red", "Green", "Blue"];
colors.push("Yellow");
console.log(colors);
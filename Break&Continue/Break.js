// Break

/* 
    # What is Break?
    - It's keyword.

    # Why use Break?
    = It's use for "jumps out" of a loop.
*/

for (let i = 0; i < 10; i++) {
  if (i === 5) {
    break;
  }
  console.log(i);
}

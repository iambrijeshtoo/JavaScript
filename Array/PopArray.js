// Pop Method In Array

/* 
    # Syntax
    datatype arrayName = [Item1,Item2, Item3];
    arrayName.pop(value);

    !Note:
    - It will only remove element at the last in the array.
*/

const colors = ["Red", "Green", "Blue"];
colors.pop("Blue");
console.log(colors);

/* 
NOTE:
- JavaScript is dynamic language so that we don't need to specify each and every datatype. It will dynamically set the datatype according the the variable's value.
*/

// Number (Same as int, float and double)
let int = 123;
let float = 3.14;
let double = 99.99;
let scientific_number = 10e2;

// BigInt (BigInt numbers can only perform operation with BigInt datatype)
let big_int_function = BigInt(1000);
let big_int_n = 1000n;

// String
let str1 = `Backtick`; 
let str2 = "Single quote"; 
let str3 = "Double quote";

// Boolean
let t = true;
let f = false;

// Object
const obj = {
  key1 : "value1",
  key2 : "value2",
  key3 : "value3",
};

// Array
let arr  = [1, 2, 3 ,4 ,5];

// Null
let my_null = null;

// Undefined
let my_undefined = undefined;
let also_undefined;

// Empty value
let empty_value = "";

// Adding string with number JavaScript treat number as a string.
let a = "abc" + 1 + 2;
console.log(a);

// JavaScript evaluate code from left to right so it will do the arithmetic operation first than treat the answer as a string. 
let b = 1 + 2 + "abc";
console.log(b);
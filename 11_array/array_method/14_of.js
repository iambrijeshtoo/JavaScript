// of()

// What
// - When you have multiple values and want to convert into one single array.

const hero1 = "Batman";
const hero2 = "Flash";
const hero3 = "Superman";

const allHero = Array.of(hero1, hero2, hero3);
console.log(allHero);

// Unary Operator `+`

// What
// - Unary Operator is nothing but `+` symbol which use to convert number which written in string format to number.

// Example
const numberAsString = "123";
const convertTonumber = +numberAsString;
console.log(typeof convertTonumber);

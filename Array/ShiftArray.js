// Shift Method In Array

/* 
    # Syntax
    datatype arrayName = [Item1,Item2, Item3];
    arrayName.shift(value);

    !Note:
    - It will only remove element at the first in the array.
*/
const colors = ["Red", "Green", "Blue"];
colors.shift("Yellow");
console.log(colors);
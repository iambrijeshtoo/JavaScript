// Continue

/* 
    # What is Continue?
    - It's keyword.

    # Why use Continue?
    - It's use for "jumps over" one iteration in the loop.
*/
for (let i = 0; i < 10; i++) {
  if (i === 5) {
    continue;
  }
  console.log(i);
}
